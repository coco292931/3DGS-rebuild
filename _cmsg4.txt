优化结果 + 第二段视频验证 + 完整说明文档

一、优化：CUDA 后端下按官方量级重训，同时启用 opacity reset
  torch 后端 1500 步峰值        22.51 dB
  cuda  后端 6000 步（无 reset） 26.82 dB
  cuda  后端 30000 步（有 reset）27.66 dB / SSIM 0.900，高斯 60102
  单视角 24.92 / 35.04 / 18.81 dB

  注：之前禁用 opacity reset 是因为 torch 后端规模小、reset 后没有足够步数恢复；
  步数够了之后它按预期发挥作用（每 3000 步一次，清理又大又透明的垃圾高斯）。

二、第二段视频（木桩特写 VID_20260910_230203）：
  SfM 141/152 帧注册，11181 个三维点，重投影误差 0.620px
  对齐旋转 109 度（这已是第四段视频都是倒的，SfM 坐标系朝向随机彻底坐实）
  cuda 后端 30000 步 -> 31.14 dB / SSIM 0.9496（基线 14.03）
  训练曲线全程单调上升：27.45 -> 27.93 -> 29.75 -> 30.30 -> 30.99 -> 31.14
  单视角 13.43 / 33.49 / 27.92 dB，视觉上已与实拍难以区分

三、新增 docs/完整说明.md（233 行，七章）：
  运行环境（硬件/软件/依赖/编译）、如何使用（三种跑法 + 两个后端的选择）、
  底层原理（场景表示、投影数学、alpha 合成、自适应密度控制、两种光栅化的差别、
  真实场景前处理）、用到了什么（自写模块 / 第三方 / 验证手段）、已知限制。

交付目录新增 output/deliverable_real4/（第二段视频的成品）。